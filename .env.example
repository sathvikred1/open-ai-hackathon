# Server-side only. Never prefix this key with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional override. Defaults to the latest model selected for this integration.
OPENAI_REPLANNING_MODEL=gpt-5.6-sol

# Optional chat-model override.
OPENAI_CHAT_MODEL=gpt-5.6-sol
